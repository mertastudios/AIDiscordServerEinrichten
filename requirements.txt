# AIDiscordServerEinrichten — Python-Abhängigkeiten
# discord.py bringt aiohttp bereits mit (wird für den Web-Server wiederverwendet,
# dadurch läuft Bot + API in EINEM Prozess und EINEM Event-Loop).
discord.py==2.7.1
aiohttp==3.14.3
